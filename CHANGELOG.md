# Changelog

Все значимые изменения в проекте **World of Empires** (client) документируются в этом файле.

---

## [0.3.0] — 12.02.2025

**Новая функциональность** | Настройки карты, пресеты, расширенный MapConfig

### Добавлено

- **game/types/game-types.ts** — расширенный MapConfig:
  - `LandMassType` (pangaea, continents, archipelago, lakes, fractal)
  - `BiomeWeights` — доли биомов (snow, tundra, taiga, grass, plains, desert)
  - `MAP_PRESETS` — 8 пресетов (default, pangaea, archipelago, desert_world, ice_age, lakes, tropical, tundra_world)
  - `getDefaultConfig(width, height)` — конфиг по умолчанию
  - `applyPreset(base, presetName)` — применение пресета
- **game/constants/dimensions.ts** — `DEFAULT_MAP_W`, `DEFAULT_MAP_H` (разрыв циклической зависимости)
- **modules/game/ui/settings-panel.tsx** — панель настройки карты:
  - Пресеты, форма суши, доля воды, климат, доли биомов
  - Статистика по биомам (%)
- **GameHud** — кнопка «⚙️ Настройки», интеграция SettingsPanel

### Изменено

- **map-generator.ts** — рефакторинг под новый MapConfig:
  - `applyLandShape` — поддержка LandMassType (PANGAEA, CONTINENTS, ARCHIPELAGO, LAKES, FRACTAL)
  - `assignBiomes` — учёт biomeWeights
  - `applyTemperature`, `applyMoisture` — temperatureBias, moistureBias
- **game-canvas.tsx** — использование GameHud, config, stats, regenerate с apply
- **game-hud.tsx** — упрощён, кнопка настроек, Legend, Hint
- **map-constants.ts** — удалены dimensions (перенесены в dimensions.ts)
- **constants/index.ts** — экспорт dimensions

### Исправлено

- Циклическая зависимость game-types → constants → map-constants → types

---

## [0.2.2] — 12.02.2025

**Исправлено** | Водные зоны в map-generator

### Изменено

- **map-generator.ts** — рефакторинг и исправление водных зон:
  - Разбиение на функции: `applyIslandFalloff`, `applyTemperatureGradient`, `assignBaseBiomes`, `applyWaterZones`, `enforceShallowBorder`, `applyTransitions`
  - BFS по 8 направлениям для водных зон (dist 1=Shallow, 2–4=Sea, >4=Ocean)
  - Валидация `enforceShallowBorder` — Shallow всегда между сушей и Sea/Ocean
  - Импорт через `../noise` (Public API)

---

## [0.2.1] — 12.02.2025

**Рефакторинг** | Архитектура, Public API, kebab-case

### Изменено

#### Архитектура

- **game/** — чистая игровая логика (без React, без Pixi):
  - `types/game-types.ts`, `constants/map-constants.ts`, `noise/noise.ts`, `generator/map-generator.ts`
  - `index.ts` — только re-export
- **engine/** — игровой движок (Pixi, DOM, без React):
  - `camera.ts`, `map-renderer.ts`, `texture-loader.ts`, `tile-factory.ts`, `constants.ts`
  - `index.ts` — только re-export
- **modules/** — React-компоненты и страницы:
  - `home/ui/`, `game/ui/` — kebab-case: `home-page.tsx`, `game-canvas.tsx`, `game-hud.tsx` и др.
  - `index.ts` — только re-export (Public API)

#### Конвенции

- Index-файлы выполняют только re-export (Public API)
- Имена файлов в kebab-case (`game-canvas.tsx`, `loading-overlay.tsx` и т.п.)

### Удалено

- **shared/game** — заменён на `game/` + `engine/`
- **shared/components** — заменён на `modules/home` + `modules/game`

---

## [0.2.0] — 12.02.2025

**Фаза 4–5** | Тайловая карта, процедурная генерация, камера, интерактивность

### Добавлено

#### Фаза 4: Ассеты и тайлы

- **texture-loader.ts** — загрузка текстур через Pixi.js Assets:
  - Регистрация bundle по `TILE_TEXTURE_PATHS`
  - Кэш текстур по типу `TileType`
  - Fallback загрузка по одной при ошибке bundle
- **tile-factory.ts** — создание тайлов:
  - Sprite с PNG-текстурой (поддержка квадратных и изометрических)
  - Fallback: цветной ромб через `Graphics` при отсутствии текстуры
- **plains.png** — новая текстура равнин (в дополнение к desert, grass, ocean и др.)
- **constants/map.ts** — `TILE_TEXTURE_PATHS`, `TILE_COLORS`, `TILE_NAMES` для 9 биомов

#### Фаза 5: Игровая логика

- **map-generator.ts** — процедурная генерация карты:
  - Perlin-like шум (elevation, temperature, moisture)
  - Island falloff — островная форма
  - Водные зоны (ocean → sea → shallow) по расстоянию от суши
  - Переходы между биомами (transitions)
- **noise.ts** — утилиты:
  - `Rng` — seeded random (LCG)
  - `noise2D` — value noise с октавами
  - `normalize` — нормализация 2D-массива
- **types** — `TileType` enum, `MapConfig`, `IsoPoint`
- **map-renderer.ts** — рендер изометрической карты:
  - `gridToIso` — преобразование сетки в изометрические координаты
  - Отрисовка тайлов 64×64
  - Hover highlight (белая обводка)
  - `onHover`, `onHoverOut`, `onClick` — колбэки
  - Hit area для ромбовидного тайла
- **camera.ts** — камера:
  - Drag (перетаскивание)
  - Zoom (колесо мыши)
  - `centerOn` — центрирование на точке
  - `minZoom` / `maxZoom`
- **iso-renderer.ts** — альтернативный загрузчик текстур (экспорт)

#### Компоненты (обновления)

- **GameCanvas**:
  - Boot-цикл: загрузка текстур → генерация карты → MapRenderer
  - Кнопка «🔄 Новая карта» (regen с новым seed)
  - HoverInfo — подсказка при наведении (биом, координаты)
  - Legend — легенда биомов с цветами
  - Hint — «Drag — перемещение | Scroll — зум»
  - LoadingOverlay — «Загрузка текстур и генерация мира...»
- **GameLoader** — упрощён (только dynamic GameCanvas + loading state)

### Изменено

- Фон canvas: `0x070d1a` (тёмно-синий)
- Модуль `shared/game` — экспорт core, constants, types, utils

---

## [0.1.0] — 12.02.2025

**Фаза 0–2** | Инициализация, маршрутизация, игровой движок

### Добавлено

#### Фаза 0: Инициализация проекта

- **Next.js 16.1.6** — базовый фреймворк приложения
- **React 19.2.3** — UI библиотека
- **TypeScript** — типизация
- **Bun** — пакетный менеджер и runtime
- **Tailwind CSS v4** — стилизация через PostCSS
- **Pixi.js 8.16.0** — 2D рендеринг для игрового canvas

#### Фаза 1: Структура приложения и маршрутизация

- **App Router** — маршрутизация Next.js
  - `/` — главная страница с приветствием и кнопкой «Start Game»
  - `/game` — игровая страница с canvas
- **Layout** — корневой layout с шрифтами Geist / Geist Mono
- **Game Layout** — отдельный layout для `/game` (fullscreen, без overflow)

#### Фаза 2–3: Компоненты и HUD

- **GameLoader** — обёртка для игрового экрана:
  - Динамический импорт `GameCanvas` (SSR отключён)
  - Экран загрузки «Загрузка игры...» при подгрузке canvas
  - HUD: кнопка «← Выход» (переход на `/`)
  - HUD: заглушка «Счёт: 0» в правом верхнем углу

- **GameCanvas** — игровой canvas на Pixi.js:
  - Инициализация Application с fullscreen (resizeTo: window)
  - Адаптивное разрешение (devicePixelRatio, autoDensity)
  - Фиксированный canvas поверх viewport
  - Демо: вращающийся зелёный квадрат (200×200)
  - Game loop через `app.ticker`
  - Корректная очистка при размонтировании

#### Стили и темы

- **globals.css** — Tailwind, CSS-переменные, тёмная тема по `prefers-color-scheme`

#### Фаза 4: Ассеты (подготовка)

- **Тайлы** (`public/assets/tilles/`):
  - desert.png, dessert.png, grass.png, ocean.png, plant.png
  - sea.png, shallow.png, snow.png, taiga.png, tundra.png

#### Конфигурация

- **ESLint** — next/core-web-vitals, next/typescript, кастомные правила
- **Prettier** — форматирование, сортировка импортов, Tailwind plugin
- **tsconfig** — path alias `@/*` → `./src/*`
- **postcss.config.mjs** — Tailwind PostCSS

#### Модули

- **modules/game** — заготовка модуля (пустой `index.ts`)
- **shared/components** — экспорт UI-компонентов

### Технические заметки

- Canvas подключается только на клиенте (`'use client'`, `dynamic` с `ssr: false`)
- Защита от двойной инициализации через `initCalled` ref
- Проверка `container.isConnected` перед добавлением canvas в DOM

---

## Формат версий

Проект использует [Semantic Versioning](https://semver.org/).

| Версия | Дата       | Фазы                                    |
|--------|------------|-----------------------------------------|
| 0.3.0  | 12.02.2025 | Настройки карты, пресеты, MapConfig     |
| 0.2.2  | 12.02.2025 | Исправление водных зон                  |
| 0.2.1  | 12.02.2025 | Рефакторинг архитектуры                 |
| 0.2.0  | 12.02.2025 | 4, 5                                    |
| 0.1.0  | 12.02.2025 | 0, 1, 2, 3 (частично)                   |

[0.3.0]: https://github.com/your-org/world-of-empires/releases/tag/v0.3.0
[0.2.2]: https://github.com/your-org/world-of-empires/releases/tag/v0.2.2
[0.2.1]: https://github.com/your-org/world-of-empires/releases/tag/v0.2.1
[0.2.0]: https://github.com/your-org/world-of-empires/releases/tag/v0.2.0
[0.1.0]: https://github.com/your-org/world-of-empires/releases/tag/v0.1.0
